import AdminLogin from "@/components/login/login";

export const metadata = {
    title: "Admin Login | FORHU",
    description: "Sign in to the FORHU Admin Portal",
};

export default function LoginPage() {
    return <AdminLogin />;
}
